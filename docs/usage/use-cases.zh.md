# 使用场景

## 个人 AI 开发者

手机上用飞书/Telegram 远程写代码，绑定你的项目。读文件、改代码、跑测试、提交 — 全部从手机操作。

## 多 Agent 团队

前端 Bot、后端 Bot、运维 Bot，各自独立工作空间，通过 Agent 总线协作。用 `mb talk` 跨 Bot 委派任务。

## 自生长的组织

管理者 Bot 通过 MetaSkill 按需创建新 Agent，分配任务，安排后续跟进。组织按需求自动扩展。

## 自主研究流水线

Agent 搜索、分析、将发现存入 MetaMemory、安排下一步。自动构建不断增长的知识库。
